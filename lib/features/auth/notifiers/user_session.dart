import 'package:flutter/foundation.dart';

class UserSession with ChangeNotifier {
  Future init() {
    // checks if the user is already logged-in when launching the app
  }

  Future login() {}

  Future logout() {}
}
