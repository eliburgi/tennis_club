import '../models/user.dart';

class LogoutUseCase {
  Future logout(User user) {
    // TODO: check that user is logged in
    // TODO: delete local token so user has to login again next time
    throw UnimplementedError();
  }
}
