import 'package:flutter/material.dart';

class HomeIcon extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Icon(Icons.home);
  }
}

class ReservationsIcon extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Icon(Icons.calendar_today);
  }
}

class MatchFinderIcon extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Icon(Icons.whatshot);
  }
}

class TournamentIcon extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Icon(Icons.weekend);
  }
}

class ProfileIcon extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Icon(Icons.person_outline);
  }
}
